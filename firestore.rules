rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Rules for all collections
    match /{document=**} {
      allow read, write: if true; // ⚠️ WARNING: For testing only. Not for production.
    }
  }
}
